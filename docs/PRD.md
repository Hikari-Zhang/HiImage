# ClearWaterMark 产品需求文档 (PRD)

> **版本**: 2.0  
> **最后更新**: 2026-05-05  
> **作者**: Hikari  
> **状态**: 开发中

---

## 1. 产品概述

### 1.1 产品定位

ClearWaterMark 是一款 AI 驱动的图像处理桌面工具，主要面向需要批量去除水印、提升图像分辨率的个人用户和设计师。产品提供简洁直观的操作界面，通过深度学习模型实现高质量的图像修复与增强。

### 1.2 目标用户

| 用户群体 | 典型场景 |
|----------|----------|
| 个人用户 | 处理个人照片上的时间戳水印、平台水印 |
| 设计师 / 创作者 | 处理素材图片、提升低分辨率素材清晰度 |
| 内容运营 | 批量处理营销素材图片 |
| 开发者 | 自部署后端服务，集成到自有工作流 |

### 1.3 产品目标

- **易用性**: 三步完成水印去除（打开 → 选区 → 处理）
- **高质量**: 基于 15+ 个 AI 模型，覆盖各类场景
- **灵活部署**: 支持本地运行和远程 Docker 部署
- **可扩展**: 模块化架构，方便未来添加更多图像处理功能

---

## 2. 功能需求

### 2.1 核心功能模块

```
┌────────────────────────────────────────────┐
│               ClearWaterMark                │
├─────────────┬──────────────┬───────────────┤
│  去水印      │  超分辨率     │  [未来扩展]   │
├─────────────┴──────────────┴───────────────┤
│               设置 / 日志                    │
└────────────────────────────────────────────┘
```

---

### 2.2 去水印 (Watermark Removal)

#### 功能描述

用户加载图片后，通过手动框选或自动检测标记水印区域，选择 AI 模型执行修复，获得去除水印后的图片。

#### 用户故事

- 作为用户，我希望能拖拽图片到应用中快速打开
- 作为用户，我希望能在图片上绘制矩形框标记水印位置
- 作为用户，我希望应用能自动检测图片中的水印区域
- 作为用户，我希望能选择不同的 AI 模型来处理不同类型的水印
- 作为用户，我希望看到处理前后的对比效果

#### 功能点

| # | 功能 | 优先级 | 描述 |
|---|------|--------|------|
| F1 | 图片加载 | P0 | 支持拖拽 / 文件对话框打开图片（PNG/JPG/BMP/WebP/TIFF） |
| F2 | ROI 绘制 | P0 | 鼠标拖拽在画布上绘制矩形选区，支持缩放/平移画布 |
| F3 | ROI 管理 | P0 | 列表展示所有选区，支持多选删除、清空所有 |
| F4 | 自动检测 | P1 | 基于 CV 算法自动识别水印位置，敏感度可调 |
| F5 | 模型选择 | P0 | 分组下拉选择模型（快速修复 / 专用模型 / 扩散模型） |
| F6 | 执行修复 | P0 | 点击按钮开始处理，显示实时进度条 |
| F7 | 结果预览 | P1 | 前后对比滑块 (Before/After) |
| F8 | 保存结果 | P0 | 通过系统对话框保存为 PNG/JPG |

#### 支持的模型

| 类别 | 模型 | 特点 |
|------|------|------|
| 快速修复 | LaMa | 推荐·通用，综合最佳 |
| 快速修复 | MiGAN | GAN 生成，速度快 |
| 快速修复 | ZITS | 边缘感知 |
| 快速修复 | MAT | 多尺度注意力，质量最高 |
| 快速修复 | CV2 | 传统算法，速度最快 |
| 专用模型 | AnyText | 文字水印专用 |
| 扩散模型 | SD Inpainting | 复杂背景 |
| 扩散模型 | Realistic Vision | 写实照片 |
| 扩散模型 | PowerPaintV2 | 文字引导，最强通用 |
| 扩散模型 | SDXL Inpainting | 高分辨率 |

---

### 2.3 超分辨率 (Super Resolution)

#### 功能描述

用户加载低分辨率图片，选择放大倍率和模型，一键生成高分辨率图片。

#### 用户故事

- 作为用户，我希望能将模糊/低分辨率图片放大并增强清晰度
- 作为用户，我希望看到放大前后的对比效果和输出尺寸预估
- 作为用户，我希望有针对动漫/插画的专用模型

#### 功能点

| # | 功能 | 优先级 | 描述 |
|---|------|--------|------|
| F9 | 图片加载 | P0 | 同去水印模块 |
| F10 | 模型选择 | P0 | 4x 通用 / 4x 动漫 / 2x 通用 |
| F11 | 尺寸预估 | P1 | 实时显示输入/输出尺寸和放大倍率 |
| F12 | 执行超分 | P0 | 进度显示 + 异步处理 |
| F13 | 对比预览 | P1 | 拖拽滑块对比原图与超分结果 |
| F14 | 保存结果 | P0 | 同去水印模块 |

#### 支持的模型

| 模型 | 倍率 | 适用场景 |
|------|------|----------|
| RealESRGAN_x4plus | 4x | 通用照片（推荐） |
| RealESRGAN_x4plus_anime_6B | 4x | 动漫/插画 |
| RealESRGAN_x2plus | 2x | 适度放大 |

---

### 2.4 日志 (Logs)

#### 功能描述

实时展示后端运行日志，方便用户排查问题。重点展示关键操作日志和所有错误信息。

#### 功能点

| # | 功能 | 优先级 | 描述 |
|---|------|--------|------|
| F15 | 实时日志流 | P0 | WebSocket 实时推送，新日志即时展示 |
| F16 | 级别过滤 | P0 | 支持按 ERROR/WARNING/INFO/DEBUG 过滤 |
| F17 | 错误高亮 | P0 | ERROR 行红色背景，WARNING 行橙色背景 |
| F18 | 错误统计 | P1 | 顶栏实时显示 Error 和 Warning 计数 |
| F19 | 自动滚动 | P1 | 可开关的自动滚动到最新日志 |
| F20 | 清空日志 | P1 | 一键清空所有历史日志 |

---

### 2.5 设置 (Settings)

#### 功能描述

管理应用的全局配置，包括连接方式、计算设备、服务器参数、网络配置等。

#### 功能点

| # | 功能 | 优先级 | 描述 |
|---|------|--------|------|
| F21 | 连接方式 | P0 | 本地启动 / 远程连接 两种模式切换 |
| F22 | 远程配置 | P0 | 输入 IP + 端口，支持测试连接验证 |
| F23 | 计算设备 | P0 | MPS (Apple Silicon) / CPU / CUDA 选择 |
| F24 | 服务器配置 | P1 | IOPaint 端口、保活时间、启动超时 |
| F25 | 网络配置 | P1 | HuggingFace Endpoint 和 Token |
| F26 | 默认参数 | P2 | 默认遮罩扩张像素数 |
| F27 | 持久化 | P0 | 配置自动保存，重启后恢复 |

---

## 3. 非功能需求

### 3.1 性能

| 指标 | 要求 |
|------|------|
| 应用启动时间 | < 3s（不含模型加载） |
| 快速模型处理 | < 30s（1920x1080 图片） |
| UI 响应 | 处理过程中界面不卡顿（异步处理） |
| 内存占用 | 空闲时 < 200MB（不含模型） |

### 3.2 兼容性

| 平台 | 支持情况 |
|------|----------|
| macOS (Apple Silicon) | 主要支持，MPS 加速 |
| macOS (Intel) | 支持，CPU 模式 |
| Windows | 计划支持（CUDA 加速） |
| Linux | Docker 部署支持 |

### 3.3 安全性

- HuggingFace Token 本地加密存储
- 后端仅监听 localhost（本地模式）
- Docker 部署时建议配合 VPN/内网使用

### 3.4 可用性

- 首次使用无需配置即可运行（默认 LaMa + MPS）
- 模型首次使用时自动下载，显示下载进度
- 操作失败时提供明确错误信息和建议

---

## 4. 技术架构

### 4.1 系统架构

```
┌────────────────────────────────────────────────────────┐
│                    Electron App                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │           React + TypeScript (Renderer)            │  │
│  │  ┌────────┐ ┌──────────┐ ┌──────┐ ┌──────────┐  │  │
│  │  │去水印   │ │超分辨率   │ │日志  │ │设置      │  │  │
│  │  └────────┘ └──────────┘ └──────┘ └──────────┘  │  │
│  │              │ Zustand State │                     │  │
│  └──────────────┼──────────────┼─────────────────────┘  │
│                 │  HTTP/WS     │                         │
│  ┌──────────────▼──────────────▼─────────────────────┐  │
│  │         Backend Manager (Main Process)             │  │
│  │         mode: local | remote                       │  │
│  └────────────────────────────────────────────────────┘  │
└────────────────────────────┬─────────────────────────────┘
                             │
              ┌──────────────▼──────────────┐
              │   Python FastAPI Backend      │
              │   (本地进程 / Docker / 远程)   │
              │                              │
              │  ┌─────────┐ ┌───────────┐  │
              │  │Inpainter│ │ Upscaler  │  │
              │  │(IOPaint) │ │(ESRGAN)   │  │
              │  └─────────┘ └───────────┘  │
              │  ┌──────────────────────┐   │
              │  │ Watermark Detector   │   │
              │  └──────────────────────┘   │
              └─────────────────────────────┘
```

### 4.2 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端框架 | Electron + React 18 + TypeScript | 桌面跨平台 |
| 构建工具 | electron-vite (Vite) | 快速 HMR |
| UI 样式 | Tailwind CSS 3 | 暗色主题 |
| 状态管理 | Zustand | 轻量 TS-first |
| 画布 | Konva.js | 图像展示 + ROI 绘制 |
| 后端框架 | Python FastAPI | 高性能异步 |
| AI 推理 | IOPaint / Real-ESRGAN | 模型推理引擎 |
| 通信协议 | REST + WebSocket | 请求/响应 + 实时推送 |
| 容器化 | Docker + docker-compose | 远程部署 |
| 打包 | PyInstaller + electron-builder | 生产发布 |

### 4.3 API 设计

| Method | Endpoint | 描述 |
|--------|----------|------|
| GET | /api/health | 健康检查 |
| GET | /api/models/inpaint | 修复模型列表 |
| GET | /api/models/upscale | 超分模型列表 |
| POST | /api/detect | 自动检测水印 |
| POST | /api/inpaint | ROI 方式去水印 |
| POST | /api/inpaint/with-mask | 掩码方式去水印 |
| POST | /api/upscale | 超分辨率处理 |
| GET | /api/settings | 获取设置 |
| PUT | /api/settings | 更新设置 |
| GET | /api/logs | 获取历史日志 |
| DELETE | /api/logs | 清空日志 |
| WS | /ws/progress | 处理进度推送 |
| WS | /api/ws/logs | 实时日志推送 |

---

## 5. 用户界面

### 5.1 设计原则

- **暗色主题**: VS Code / Discord 风格，减少视觉疲劳
- **信息层级**: 主内容区大面积展示，控制面板紧凑收纳
- **即时反馈**: 操作后立即响应（进度条、状态提示）
- **最少点击**: 常用操作 1-2 步完成

### 5.2 页面结构

```
┌─────────┬──────────────────────────────┐
│         │         Top Bar              │
│ Sidebar │──────────────────────────────│
│         │                              │
│ 去水印   │     Main Content Area        │
│ 超分辨率 │     (Canvas / Form / Table)   │
│ 日志     │                              │
│ 设置     │                              │
│         │                              │
│ ─────── │                              │
│ Status  │                              │
└─────────┴──────────────────────────────┘
```

### 5.3 交互规范

| 交互 | 行为 |
|------|------|
| 侧边栏折叠 | 点击 `<` 按钮折叠为图标模式 (56px) |
| 图片打开 | 拖拽到画布区 或 点击按钮打开文件对话框 |
| ROI 绘制 | 选择绘制工具后，鼠标拖拽创建矩形 |
| 画布缩放 | 鼠标滚轮缩放，锚点在鼠标位置 |
| 画布平移 | 切换到平移工具后鼠标拖拽 |
| 进度反馈 | 进度条 + 百分比 + 状态文字 |
| 错误提示 | Toast 通知 + 日志页记录 |

---

## 6. 部署方案

### 6.1 本地模式（默认）

```bash
# 开发环境
./scripts/dev.sh

# 生产环境
# Electron app 内置 PyInstaller 打包的 backend
# 双击 .app 即可使用
```

### 6.2 Docker 远程部署

```bash
# 服务器端
git clone <repo>
cd ClearWaterMark
docker compose up -d

# 客户端
# 在设置中选择"远程连接"，输入服务器 IP:8787
```

### 6.3 发布形态

| 形态 | 说明 |
|------|------|
| macOS .dmg | Apple Silicon arm64，含内置后端 |
| Docker Image | linux/amd64，GPU 可选 |
| 源码 | 开发者自行构建 |

---

## 7. 里程碑计划

| 阶段 | 时间 | 目标 |
|------|------|------|
| M1: 后端 API | Week 1 | FastAPI 全部接口可用，核心逻辑复用 |
| M2: 前端骨架 | Week 1-2 | Electron + React + 路由 + 暗色主题 |
| M3: 去水印页面 | Week 2-3 | 画布、ROI、模型选择、处理流程完整 |
| M4: 超分辨率页面 | Week 3 | 超分处理 + 对比预览 |
| M5: 日志 + 设置 | Week 3 | 实时日志 + Docker + 连接配置 |
| M6: 打包发布 | Week 4 | PyInstaller + electron-builder + .dmg |

---

## 8. 未来规划

### 8.1 Phase 2 (计划中)

| 功能 | 描述 |
|------|------|
| 批量处理 | 多图队列处理，自动检测 + 去除 |
| 智能检测 v2 | 集成 YOLO/SAM 模型，识别更多水印类型 |
| FFT 频域分析 | 检测平铺/重复水印（全图半透明水印） |
| 撤销/重做 | 操作历史栈 |

### 8.2 Phase 3 (远期)

| 功能 | 描述 |
|------|------|
| 图片降噪 | AI 去噪声 |
| 背景移除 | AI 抠图 |
| 色彩增强 | 自动调色/HDR |
| 图片修复 | 老照片修复 |
| 插件系统 | 第三方模型/处理器接入 |

---

## 9. 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 大模型下载慢 | 首次使用体验差 | 提供 HF Mirror 选项 + 下载进度显示 |
| MPS 内存不足 | 大图处理失败 | 自动降级到 CPU + 分块处理 |
| Docker GPU 兼容 | 特定显卡不支持 | 提供 CPU fallback 配置 |
| 模型更新 | 版本不兼容 | 模型版本锁定 + 自动下载指定版本 |

---

## 附录

### A. 术语表

| 术语 | 说明 |
|------|------|
| ROI | Region of Interest，用户标记的水印区域 |
| Inpainting | 图像修复，将水印区域重建为正常背景 |
| Super Resolution | 超分辨率，通过 AI 放大图像并增强细节 |
| IOPaint | 开源图像修复框架，支持多种模型 |
| Real-ESRGAN | 超分辨率模型，支持 2x/4x 放大 |
| MPS | Metal Performance Shaders，Apple 芯片 GPU 加速 |
| Dilation | 遮罩扩张，让遮罩略大于 ROI 确保完全覆盖水印边缘 |

### B. 参考资料

- [IOPaint Documentation](https://github.com/Sanster/IOPaint)
- [Real-ESRGAN](https://github.com/xinntao/Real-ESRGAN)
- [Electron Documentation](https://www.electronjs.org/docs)
- [FastAPI Documentation](https://fastapi.tiangolo.com)
