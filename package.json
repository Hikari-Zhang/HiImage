{
  "private": true,
  "scripts": {
    "dev": "./scripts/dev.sh",
    "backend:dev": "cd backend && ../venv/bin/python -m uvicorn app.main:app --reload --port 8787",
    "frontend:dev": "cd frontend && npm run dev"
  }
}
